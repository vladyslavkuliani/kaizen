<h1><%= @manager.name %></h1>

<br>

Projects:<br>
<% @projects.each do |project| %>
  <p>
    <%= link_to project.title, "/projects/#{project.title}" %><br>
    <%= project.deadline %><br>
    <%= project.description %><br>
  </p>
<% end %>
